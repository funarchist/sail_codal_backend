(executable
 (name sail_plugin_codal)
 (modes
  (native plugin)
  (byte plugin))
 (libraries libsail))

(install
 (section
  (site
   (libsail plugins)))
 (package sail_codal_backend)
 (files sail_plugin_codal.cmxs sail_plugin_codal.cma))